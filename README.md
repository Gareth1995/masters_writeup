# masters_writeup

If you run the front_page.rmd file it will generate the whole dissertation document. The files of main importance are the .rmd files. These are the ones used by front_page.rmd to generate the whole document
